Java 虚拟机在运行的时候会占用一块内存空间，为了更好地利用这块空间，虚拟机将其分为了五个部分：

- 栈内存：每个线程都有自己独立的栈，目前只需考虑一个即可，方法被调用进栈执行，执行完毕出栈。
- 堆内存：所有线程共享，存储对象、数组、字符串常量池。(new关键字)
- 方法区：JDK7以前由永久代实现，JDK8以后由元空间实现，从虚拟机内部移动到本地内存。用来存储字节码信息、常量、静态变量。
- 本地方法栈：调用本地 Native 方法。(平时用不到)
- 程序计数器：每个线程独立，记录当前线程执行的字节码指令地址(行号)。给 CPU 用的，平时用不到。